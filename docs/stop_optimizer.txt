This file serves as a signal to `Excel_Solver.optimize()` method to STOP the optimization.
Move this file to the same directory as `optimizer.py` to STOP the optimization and finish the remainder of the script.
Once the optimization is stopped, move the file back to the original location so that the next optimization can run without interruption.
